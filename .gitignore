# Dependencies
node_modules/
.pnp
.pnp.*

# Next.js / build output
.next/
out/
build/
dist/
*.tsbuildinfo

# Environment / secrets  (keeps .env.example tracked)
.env
.env.local
.env.*.local

# SSH / private keys — NEVER commit these (keys live in ~/.ssh, outside the repo)
*.pem
*.key
id_rsa
id_ed25519

# Testing
coverage/
playwright-report/
test-results/

# Logs
*.log
pnpm-debug.log*

# OS / editor
.DS_Store
.vscode/
.idea/

# Local-only knowledge folders — never on GitHub (ops runbooks with infra details,
# planning notes, reference material, and the agent-onboarding context corpus)
docs/
plan/
references/
prompts/
tasks/

# Claude Code personal settings (machine-specific paths)
.claude/settings.local.json
